<Window x:Class="TreeType.ToggleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TreeType" Height="104.46" Width="196.4" Topmost="True"
        ResizeMode="NoResize" WindowStartupLocation="Manual" ShowInTaskbar="False"
        ShowActivated="False">
    <Grid Margin="0,0,0,5">
        <Button Content="Exit" HorizontalAlignment="Left" Margin="98.4,10,0,0" 
                VerticalAlignment="Top" Width="82" Name="exit" Click="exitListener"/>
        <TextBlock HorizontalAlignment="Left" Margin="10,36.96,0,0" TextWrapping="Wrap" Name="instructions"
                   Text="Hold the right mouse button to turn the keyboard on or off" VerticalAlignment="Top" Width="170.4"
                   TextAlignment="Center"/>
        <Button Content="Settings" HorizontalAlignment="Left" Margin="10,10,0,0" 
                Name="SettingsButton" VerticalAlignment="Top" Width="82" Click="settingsListener"/>

    </Grid>
</Window>
